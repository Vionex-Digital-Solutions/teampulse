export default function StandupPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold text-navy">Async Standup</h1>
      <p className="text-slate-600">
        TODO (frontend team): build a form with three fields — Yesterday,
        Today, Blockers — and POST it to <code>/api/v1/standup</code> using the
        API client in <code>lib/api.ts</code>.
      </p>
      <p className="text-slate-400 text-sm">
        This page is a starter stub. Make it real.
      </p>
    </div>
  );
}
